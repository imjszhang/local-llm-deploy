# 可选：在项目根目录执行 cp config/examples/hf-env.example .hf-env（已被 .gitignore）。
# 下载命令读取项目根目录的 .hf-env。
# Hugging Face Hub 镜像（与 huggingface_hub / transformers 一致）
# HF_ENDPOINT=https://hf-mirror.com
#
# 强制使用官方 Hub（覆盖下载脚本中的镜像默认值）：
# HF_ENDPOINT=https://huggingface.co
